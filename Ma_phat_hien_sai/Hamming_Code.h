#include <iostream>
#include <vector>
#include <cmath>
using namespace std;


vector<int> hammingCode(vector<int> &Data);

// Giải mã và kiểm tra lỗi mã Hamming
vector<int> decodeHammingCode(vector<int> &hammingCodeOut);

void Display_HammingCode(vector<int> &hammingCodeOut);
